[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    ui: UI tests using Playwright (deselect with '-m "not ui"')
    slow: Slow tests (deselect with '-m "not slow"')